## 1. Provider 配置与能力验证

- [x] 1.1 审计现有 AI interpretation 配置、`.gitignore` 和部署文档，确保 Qwen API key 仅来自未跟踪的服务端环境变量且不会被日志输出。
- [x] 1.2 新增 Qwen-compatible provider 配置与 adapter，保留现有 provider 接口及历史解读记录的可读性。
- [x] 1.3 为 endpoint、模型、超时、重试、图像输入、结构化 JSON 输出和文本降级定义显式能力配置与校验结果。
- [x] 1.4 实现非敏感 provider 能力探针或等价部署校验，验证目标 `qwen-plus` 的实际能力；若不支持视觉输入，要求配置一个已验证的 Qwen 视觉模型才可启用视觉模式。
- [x] 1.5 为认证失败、超时、配额限制、能力不匹配、无效 JSON 和重试行为补充 provider 单元测试。

## 2. 多模态证据投影

- [x] 2.1 审计五页报告资产 manifest、存储访问和现有关键姿态/时序资产格式，确定稳定 asset ID、revision、generation signature 与指标/finding 关联字段。
- [x] 2.2 定义版本化 `MultimodalEvidenceBundle`、evidence manifest 和曲线数值摘要 schema，并为媒体类型、关联 facts、选择理由和排除原因建模。
- [x] 2.3 实现按报告模块选择关键姿态图和时序图的逻辑，执行数量、像素、字节和总请求预算上限。
- [x] 2.4 实现时序曲线的受限下采样摘要，保留单位、横轴语义、缺失段和关联 metric facts，禁止投影完整逐帧关键点。
- [x] 2.5 在 provider 调用前校验证据资产属于当前报告、当前 revision 且调用方有权访问；对缺失、陈旧或不可读资产记录排除原因并文本降级。
- [x] 2.6 为无资产、超限资产、跨模块资产、陈旧资产、缺失数据和隐私字段排除补充单元测试。

## 3. 请求、签名与事实护栏

- [x] 3.1 扩展 AI interpretation 输入与 generation signature，使其纳入视觉模式、模型能力、evidence manifest hash、资产版本和曲线摘要版本。
- [x] 3.2 在 Qwen adapter 中实现兼容 API 的多段消息请求，发送 JSON facts/knowledge 与受控图片；文本模式不得发送图片二进制或图片 URL。
- [x] 3.3 扩展输出 schema 以支持可选 `evidence_refs`，并保持每个技术含义块的 `fact_refs` 为强制事实依据。
- [x] 3.4 扩展本地 validator，校验 evidence 引用存在、数值/单位只来自事实、不可用指标不被数值化，并拒绝视觉臆造、诊断、评分和确定性因果。
- [x] 3.5 新增审计字段或迁移，持久化非敏感 evidence manifest、能力结果、输入 hash、实际模式和排除原因，不保存图像二进制或 provider 原始请求/响应。
- [x] 3.6 实现资产或证据版本变化后的 stale 判断与同签名复用逻辑。

## 4. API、报告展示与 PDF

- [x] 4.1 扩展解读读取 API 的 trace/envelope，公开实际运行模式、已使用证据 ID 和安全的降级原因，不暴露 URL、路径或凭据。
- [x] 4.2 在 Web 五页报告中展示本次解读的文本/视觉模式和可追溯证据来源，保持现有 ready/pending/failed/stale 状态语义。
- [x] 4.3 确保打印视图和 PDF 读取同一份持久化解读与证据摘要，且打印期间不调用 provider、不增加第五页之外的分页。
- [x] 4.4 补充 API、Vue 组件、打印视图和 PDF 回归测试，覆盖视觉模式、文本降级和失败状态。
- [x] 4.5 修复二维分析进度的原始 JSON 渲染，并为 AI 解读提供可关闭的状态窗口、已等待时间与最长 150 秒轮询反馈。

## 5. 评测、运维与受控上线

- [x] 5.1 建立去标识化固定评测集，覆盖正常、低置信度、unavailable、无资产、陈旧资产、视觉/文本能力不匹配和对抗性视觉臆造场景。
- [x] 5.2 为事实一致性、有效 fact/evidence 引用、护栏拒绝率、视觉资产选择、延迟、token/图片成本与文本降级建立可观测性指标。
- [x] 5.3 更新本地开发与运维文档，说明 Qwen 配置、密钥处理、能力验证、视觉数据边界、回滚和模型切换流程。
- [x] 5.4 使用真实去标识化报告完成文本与多模态输出对照，并记录教练盲评的可读性、正确性、可执行性和误导风险。（2026-07-30：原型阶段无教练可参与，按项目负责人决定豁免盲评；已完成文本与视觉模式真实调用对照。）
- [x] 5.5 运行后端、前端、PDF 与固定评测全量测试；确认基础报告在 Qwen 未配置、失败或关闭视觉模式时仍完整可用。
- [x] 5.6 默认保持自动生成为关闭状态，完成试运行门槛和回滚负责人确认后再决定是否小范围开启。
