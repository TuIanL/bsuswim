## 1. 数据模型与迁移

- [x] 1.1 盘点训练记录删除涉及的外键和 ORM 关系，为级联删除补充必要的 `ondelete`、relationship cascade 或显式服务删除边界。
- [x] 1.2 新增 Alembic migration，创建文件清理失败记录及其状态、失败路径、错误信息和重试时间字段。
- [x] 1.3 为清理失败记录建立模型、schema 和受限的重试/查询能力，并确保其不暴露其他教练的数据。

## 2. 后端删除与存储清理

- [x] 2.1 实现训练记录删除编排服务：加载当前教练拥有的会话及完整关联图，并拒绝含非终态分析任务的删除请求。
- [x] 2.2 实现上传根目录内路径的规范化、越界和符号链接验证，以及由登记文件路径归并资产/PDF 专属目录的逻辑。
- [x] 2.3 实现数据库关联的事务性删除顺序，覆盖会话视频、标注、标准化标注、指标、资产、复核结论、任务、结果、报告和 AI 解读。
- [x] 2.4 实现共享视频引用检查：仅在无其他 `SessionVideo` 引用时删除 `VideoFile` 元数据及其原始文件。
- [x] 2.5 实现提交后文件清理、失败记录持久化和幂等重试，确保文件系统异常不会被报告为完整成功。
- [x] 2.6 新增 `DELETE /api/v1/sessions/{session_id}` 与清理重试 API，完成教练所有权、`404`、`409` 和错误响应映射。

## 3. 前端与 Demo 体验

- [x] 3.1 在 API 客户端封装删除训练记录和清理重试所需调用，并定义可读的错误映射。
- [x] 3.2 在测试任务列表添加危险删除操作、标题和资源影响明确的二次确认、提交中禁用以及成功后的会话/任务刷新。
- [x] 3.3 在运动员档案历史记录添加同一删除操作，并在成功后刷新历史表和趋势数据。
- [x] 3.4 为 demo 数据实现按会话删除，使会话、任务、会话视频及派生 demo 数据保持一致。

## 4. 测试与验证

- [x] 4.1 为删除服务编写测试，覆盖完整关联清理、无可选产物、共享视频保留、非法路径拒绝和文件清理失败记录。
- [x] 4.2 为 API 编写鉴权和状态测试，覆盖成功删除、他人/不存在记录 `404`、运行中任务 `409` 和失败清理重试。
- [x] 4.3 为前端测试任务列表和运动员档案编写删除确认、成功刷新、失败反馈及 demo 模式测试。
- [x] 4.4 在本地迁移后的数据库和真实上传目录执行端到端删除验证，确认数据库无关联残留且受控文件已删除。
